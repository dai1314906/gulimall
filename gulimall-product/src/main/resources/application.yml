spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.152.20:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  thymeleaf:
    cache: false
  redis:
    host: 192.168.152.20
    port: 6379


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml # 只扫描自己模块下的mapper文件下的.xml文件
  global-config:
    db-config:
      id-type: auto # 表示自增主键
      logic-delete-value: 1 # 1 表示已经删除
      logic-not-delete-value: 0 # 0 表示没有删除
server:
  port: 10000
logging:
  level:
    com.atguigu.gulimall: error

